# ===== Venus LLM 后端 =====
# MCP 直连 Venus（OpenAI Chat Completions 协议），战略/战术层 LLM 后端。
# 凭据必需，缺失 MCP 启动会 fail。
VENUS_API_KEY=sk-xxx
# 可选覆盖（默认值见 agenttown-mcp flag）
# VENUS_URL=http://v2.open.venus.oa.com/llmproxy
# VENUS_MODEL=deepseek-v4-flash

# ===== agenttown-mcp (host process, Go) =====
# MCP Streamable HTTP 监听地址（debug 控制台 + 健康检查）
AGENTTOWN_MCP_HTTP=:8760
# WebSocket 监听地址（Mock UE 连接此端点推送感知）
AGENTTOWN_MCP_WS=:9090
# 自动规划总开关（true=战略/战术/反应层自动决策；false=手动模式，仅响应
# /debug/schedule 注入和 /debug/action 手动下发，适合联调时隔离 UE 端）
AGENTTOWN_MCP_AUTO_PLAN=true

# ===== 反应层 LLM（本地 Ollama，默认启用）=====
# 默认启用反应层：MCP flag --ollama-url 默认 http://localhost:11435
# 云端场景：Ollama 跑在 Windows 本地，需先在 Windows 端运行 bash start-tunnel.sh
# 拉起 SSH 反向隧道，云端通过 http://localhost:11435 访问。
# Windows 原生场景：MCP 直接访问 http://localhost:11434，无需隧道。
# 禁用反应层：取消下行注释并设为空串（OLLAMA_URL=）
# OLLAMA_URL=http://localhost:11435
# OLLAMA_MODEL=qwen2.5:7b-instruct-q4_K_M
# OLLAMA_NUM_THREAD=16

# ===== SSH 反向隧道（仅 Windows 端 start-tunnel.sh 用）=====
# start-tunnel.sh 在 Windows Git Bash 运行，把云端 OLLAMA_TUNNEL_PORT 反连回
# Windows 本地 Ollama。云端 start-dev.sh 据此端口检测隧道是否建立。
# SSH_TUNNEL_REMOTE_HOST=21.214.99.100
# SSH_TUNNEL_REMOTE_PORT=36000
# SSH_TUNNEL_REMOTE_USER=root
# OLLAMA_TUNNEL_PORT=11435

# ===== 持久化存储（可选，MySQL；Stage 3）=====
# 空 = 内存模式（NoopStore，无持久化，测试/quick-smoke 默认）；
# 非空 = 启用 MySQL 持久化（4 调度字段 write-through + 预埋记忆/关系/动作历史表）。
# DSN 必须含 parseTime=true 以正确扫描 DATETIME 列。
# MYSQL_DSN=user:pass@tcp(127.0.0.1:3306)/agenttown?parseTime=true&charset=utf8mb4
